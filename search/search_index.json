{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"ddqn-router","text":"<p>A lightweight Python library that trains a Double DQN agent to route user queries to the optimal subset of specialized agents in a multi-agent system.</p> <ul> <li>Fast: sub-millisecond inference on CPU \u2014 no LLM needed at serving time.</li> <li>Practical: define agents in YAML, label a small dataset with an LLM, train,   serve. All via a single <code>ddqn-router</code> CLI or a typed Python API.</li> <li>Typed, tested, and production-ready.</li> </ul>"},{"location":"#where-to-go-next","title":"Where to go next","text":"<ul> <li>Quickstart \u2014 install and train your first router in ~10 minutes.</li> <li>CLI reference \u2014 every subcommand and its flags.</li> <li>Python API \u2014 <code>DDQNRouter</code>, <code>RouteResult</code>, <code>StepTrace</code>.</li> <li>Configuration \u2014 every YAML field and its default.</li> <li>Deployment \u2014 Docker, compose, reverse-proxy, production checklist.</li> <li>Troubleshooting \u2014 common errors.</li> </ul>"},{"location":"#install","title":"Install","text":"<pre><code>pip install ddqn-router\npip install \"ddqn-router[serve]\"   # for the FastAPI server\n</code></pre>"},{"location":"changelog/","title":"Changelog","text":""},{"location":"changelog/#changelog_1","title":"Changelog","text":"<p>All notable changes to this project are documented here. Format follows Keep a Changelog and the project follows Semantic Versioning.</p>"},{"location":"changelog/#unreleased","title":"[Unreleased]","text":""},{"location":"changelog/#030-2026-04-20","title":"[0.3.0] - 2026-04-20","text":""},{"location":"changelog/#added","title":"Added","text":"<ul> <li><code>ddqn-router init</code> \u2014 scaffolds <code>config.yaml</code>, <code>data/queries.example.txt</code>,   <code>.gitignore</code>, and a stub <code>README.md</code> in a new project directory.</li> <li><code>ddqn-router eval</code> \u2014 evaluates a trained router on a labeled JSONL dataset   (<code>--artifacts</code>, <code>--input</code>, optional <code>--output metrics.json</code>).</li> <li><code>ddqn-router train --resume &lt;checkpoint-dir&gt;</code> \u2014 resumes training from a saved   checkpoint. Online + target Q-nets, Adam state, RNG state, and the step/epsilon   metadata are restored. Opt-in <code>--save-replay</code> persists the replay buffer.</li> <li><code>training.checkpoint_freq</code> config field (default <code>10000</code>) controls checkpoint   cadence. Checkpoints land in <code>&lt;output_dir&gt;/checkpoint/</code>.</li> <li><code>DDQNRouter.route_verbose(query)</code> + <code>StepTrace</code> + <code>RouteResult.steps_trace</code>   expose per-step Q-values, selected action, and masked agents structurally.   <code>router.explain(query)</code> now reuses this trace while keeping stdout output   byte-identical to prior versions.</li> <li><code>rich</code>-based progress panels for <code>label</code> (progress bar + cache hit rate +   estimated cost) and <code>train</code> (live training panel with ETA).</li> <li>PyPI / Python / License / CI / downloads / docs badges in README.</li> <li><code>py.typed</code> marker and <code>Typing :: Typed</code> classifier.</li> <li><code>docs/</code> mkdocs-material site (quickstart, CLI/API/config reference,   deployment, troubleshooting, confidence semantics). Auto-deployed from   <code>main</code> via <code>.github/workflows/docs.yml</code>.</li> <li><code>Dockerfile</code>, <code>docker-compose.yml</code>, and <code>.github/workflows/docker.yml</code>   publishing multi-arch <code>ghcr.io/kirmoz1997/ddqn-router</code> images on each   GitHub Release.</li> <li><code>tests/</code> suite with fixtures and ~70%+ line coverage outside <code>rl/</code>   (pytest + <code>fastapi.testclient</code> for serve tests).</li> <li><code>.github/workflows/ci.yml</code> matrix: Python 3.10/3.11/3.12 \u00d7 Ubuntu/macOS.</li> <li><code>CONTRIBUTING.md</code>, <code>SECURITY.md</code>, <code>CODE_OF_CONDUCT.md</code>, issue &amp; PR templates,   <code>.pre-commit-config.yaml</code>, <code>.github/dependabot.yml</code>.</li> <li><code>examples/01_customer_support/</code> and <code>examples/02_it_helpdesk/</code> reproducible   end-to-end demos plus an <code>examples/README.md</code> index.</li> <li><code>[project.optional-dependencies] docs = [\u2026]</code>.</li> <li><code>[tool.ruff]</code>, <code>[tool.mypy]</code>, <code>[tool.pytest.ini_options]</code>,   <code>[tool.setuptools.package-data]</code> sections in <code>pyproject.toml</code>.</li> </ul>"},{"location":"changelog/#changed","title":"Changed","text":"<ul> <li>Validated <code>RouterConfig</code>: dataset ratios must sum to <code>1.0</code>; duplicate agent   IDs are now rejected at parse time; <code>step_cost</code> must be <code>&gt;= 0</code>.</li> <li><code>RouteResult</code> is now a <code>dataclass</code> (was <code>NamedTuple</code>) so it can carry an   optional <code>steps_trace</code> field. All existing attribute access   (<code>result.agents</code>, <code>.agent_names</code>, <code>.confidence</code>, <code>.steps</code>) is preserved.</li> </ul>"},{"location":"changelog/#fixed","title":"Fixed","text":"<ul> <li><code>AgentRegistry</code> now rejects duplicate IDs / names and non-contiguous ID   ranges instead of silently overwriting them.</li> </ul>"},{"location":"changelog/#021-2026-04-07","title":"[0.2.1] - 2026-04-07","text":""},{"location":"changelog/#fixed_1","title":"Fixed","text":"<ul> <li>Removed empty email from <code>authors</code> in <code>pyproject.toml</code> to satisfy PyPI   metadata validation during trusted publishing.</li> </ul>"},{"location":"changelog/#020-2026-04-07","title":"[0.2.0] - 2026-04-07","text":""},{"location":"changelog/#added_1","title":"Added","text":"<ul> <li>PyPI publishing setup: <code>pip install ddqn-router</code> now works.</li> <li><code>examples/quickstart.ipynb</code>: end-to-end runnable demo notebook   with a built-in labeled dataset (no API key required to start).</li> <li>\"Open in Colab\" badge in README pointing to the quickstart notebook.</li> <li><code>CHANGELOG.md</code>.</li> </ul>"},{"location":"changelog/#010-2026-04-07","title":"[0.1.0] - 2026-04-07","text":""},{"location":"changelog/#added_2","title":"Added","text":"<ul> <li>Core DDQN routing library.</li> <li>CLI: <code>label</code>, <code>dataset stats</code>, <code>dataset split</code>, <code>train</code>, <code>serve</code>.</li> <li>Python inference API: <code>DDQNRouter.load()</code>, <code>route()</code>, <code>route_batch()</code>,   <code>explain()</code>.</li> <li>FastAPI serve mode (optional <code>[serve]</code> extra).</li> </ul>"},{"location":"cli/","title":"CLI reference","text":"<p>All subcommands are accessible via the <code>ddqn-router</code> entry point.</p> <pre><code>$ ddqn-router --help\n</code></pre>"},{"location":"cli/#ddqn-router-init","title":"<code>ddqn-router init</code>","text":"<p>Scaffold a new project directory.</p> Flag Default Description <code>--path</code> <code>.</code> Target directory. <code>--force</code> <code>false</code> Overwrite existing files. <p>Produces <code>config.yaml</code>, <code>data/queries.example.txt</code>, <code>.gitignore</code>, <code>README.md</code>.</p>"},{"location":"cli/#ddqn-router-label","title":"<code>ddqn-router label</code>","text":"<p>Label raw queries with an LLM. Required: <code>--config</code>.</p> <p>Key flags: <code>--input</code>, <code>--output</code>, <code>--model</code>, <code>--base-url</code>, <code>--api-key</code>, <code>--min-agents</code>, <code>--max-agents</code>, <code>--prompt-template</code>, <code>--cache</code>, <code>--fallback-strategy {skip|keyword|all-agents}</code>.</p> <p>Shows a live <code>rich</code> progress bar with cache-hit rate and estimated prompt cost (for known models).</p>"},{"location":"cli/#ddqn-router-dataset-stats","title":"<code>ddqn-router dataset stats</code>","text":"<p><code>--input path/to/tasks.jsonl</code> \u2014 prints agent frequency and set-size distribution.</p>"},{"location":"cli/#ddqn-router-dataset-split","title":"<code>ddqn-router dataset split</code>","text":"<p><code>--input</code>, <code>--train 0.7</code>, <code>--val 0.15</code>, <code>--test 0.15</code>, <code>--output-dir</code>, <code>--seed 42</code>.</p> <p>Stratified by required-set size.</p>"},{"location":"cli/#ddqn-router-train","title":"<code>ddqn-router train</code>","text":"<p>Train the Double DQN router.</p> Flag Description <code>--config</code> Path to <code>router_config.yaml</code> (required). <code>--output-dir</code> Override <code>output_dir</code> from config. <code>--resume</code> Resume from a checkpoint directory (e.g. <code>./artifacts/checkpoint/</code>). <code>--save-replay</code> Persist the replay buffer alongside each checkpoint. <p>Checkpoints are written every <code>training.checkpoint_freq</code> steps (default <code>10000</code>).</p>"},{"location":"cli/#ddqn-router-eval","title":"<code>ddqn-router eval</code>","text":"<p>Evaluate a trained router on a labeled JSONL.</p> Flag Description <code>--artifacts</code> Directory containing <code>model.pt</code> etc. (default <code>./artifacts/</code>). <code>--input</code> Path to labeled JSONL (e.g. <code>test.jsonl</code>). Required. <code>--output</code> Optional path to dump metrics JSON. <p>Exit codes: <code>0</code> success, <code>2</code> missing artifacts, <code>1</code> dataset errors.</p>"},{"location":"cli/#ddqn-router-serve","title":"<code>ddqn-router serve</code>","text":"<p>Start the FastAPI server (requires <code>[serve]</code> extras).</p> Flag Default Description <code>--artifacts</code> <code>./artifacts/</code> Trained model directory. <code>--host</code> <code>0.0.0.0</code> Bind host. <code>--port</code> <code>8000</code> Bind port. <code>--cors</code> (off) Comma-separated allowed origins, or <code>*</code>. <p>Exposes <code>POST /route</code>, <code>POST /route/batch</code>, <code>GET /agents</code>, <code>GET /health</code>.</p>"},{"location":"configuration/","title":"Configuration","text":"<p><code>RouterConfig</code> is the single source of truth for every tunable knob. Load from YAML or instantiate directly.</p> <pre><code>from ddqn_router.config import RouterConfig\ncfg = RouterConfig.from_yaml(\"config.yaml\")\n</code></pre>"},{"location":"configuration/#top-level","title":"Top-level","text":"Field Default Description <code>agents</code> <code>[]</code> List of agent definitions (see below). Must be non-empty for training/serving. <code>training</code> \u2014 <code>TrainingConfig</code> block. <code>labeler</code> \u2014 <code>LabelerConfig</code> block. <code>dataset</code> \u2014 <code>DatasetConfig</code> block. <code>output_dir</code> <code>./artifacts/</code> Where to write <code>model.pt</code>, <code>encoder.joblib</code>, checkpoints."},{"location":"configuration/#agents","title":"<code>agents</code>","text":"<p>Each entry must have a unique <code>id</code> and <code>name</code>. IDs must be contiguous starting from <code>0</code> (e.g. <code>0, 1, 2</code> \u2014 not <code>0, 2, 3</code>). Duplicate IDs are rejected at load time.</p> <pre><code>agents:\n  - id: 0\n    name: \"Billing\"\n    description: \"invoice payment refund subscription\"\n</code></pre>"},{"location":"configuration/#training-trainingconfig","title":"<code>training</code> (<code>TrainingConfig</code>)","text":"Field Default Description <code>total_steps</code> <code>200000</code> Total environment steps. <code>batch_size</code> <code>64</code> Mini-batch size for the Q-network update. <code>learning_rate</code> <code>0.001</code> Adam learning rate. <code>gamma</code> <code>0.99</code> Discount factor. <code>epsilon_start</code> / <code>epsilon_end</code> <code>1.0</code> / <code>0.05</code> Linear \u03b5 decay. <code>epsilon_decay_steps</code> <code>100000</code> Steps over which \u03b5 decays. <code>target_update_freq</code> <code>500</code> Sync target \u2190 online every N steps. <code>replay_buffer_size</code> <code>50000</code> Capacity of the uniform replay buffer. <code>min_replay_size</code> <code>1000</code> Steps to warm-up before starting gradient updates. <code>reward_mode</code> <code>\"jaccard\"</code> <code>\"jaccard\"</code> or <code>\"stochastic\"</code>. <code>step_cost</code> <code>0.05</code> Per-selection penalty. Must be \u2265 0. <code>hidden_layers</code> <code>[256, 128]</code> MLP hidden dims for the Q-network. <code>tfidf_max_features</code> <code>5000</code> Vocabulary cap for the TF-IDF encoder. <code>action_masking</code> <code>true</code> Disallow re-selecting an already-chosen agent. <code>seed</code> <code>42</code> Python / numpy / torch seed. <code>val_eval_freq</code> <code>5000</code> Validation cadence in steps. <code>save_best</code> <code>true</code> Persist the best val-Jaccard checkpoint. <code>max_steps_per_episode</code> <code>20</code> Hard cap on episode length. <code>checkpoint_freq</code> <code>10000</code> Training-resume checkpoint cadence."},{"location":"configuration/#labeler-labelerconfig","title":"<code>labeler</code> (<code>LabelerConfig</code>)","text":"Field Default Description <code>model</code> <code>\"gpt-4o-mini\"</code> Any OpenAI-compatible model name. <code>base_url</code> <code>\"https://api.openai.com/v1\"</code> Swap for DeepSeek, Ollama, etc. <code>api_key</code> <code>\"\"</code> Or set <code>DDQN_ROUTER_API_KEY</code>. <code>input</code> <code>\"\"</code> Path to raw texts (jsonl or one-per-line). <code>output</code> <code>./data/tasks.jsonl</code> Labeled output path. <code>min_agents</code> <code>2</code> Minimum agents per example. <code>max_agents</code> <code>null</code> Upper cap (default: no cap). <code>prompt_template</code> <code>null</code> Override path to a Jinja2 template. <code>prompt_version</code> <code>\"v1\"</code> Cache key component \u2014 bump to invalidate cache. <code>batch_size</code> <code>1</code> Requests per batch (currently serialized). <code>cache</code> <code>./cache/label_cache.jsonl</code> Append-only JSONL cache. <code>fallback_strategy</code> <code>\"keyword\"</code> <code>skip</code> / <code>keyword</code> / <code>all-agents</code>."},{"location":"configuration/#dataset-datasetconfig","title":"<code>dataset</code> (<code>DatasetConfig</code>)","text":"Field Default Description <code>input</code> <code>./data/tasks.jsonl</code> Labeled dataset. <code>train_ratio</code> / <code>val_ratio</code> / <code>test_ratio</code> <code>0.7 / 0.15 / 0.15</code> Must sum to 1.0. <code>output_dir</code> <code>./data/</code> Where split files land."},{"location":"configuration/#validation","title":"Validation","text":"<p>Loading a config runs Pydantic validators that enforce:</p> <ul> <li><code>train_ratio + val_ratio + test_ratio == 1.0</code> (within 1e-6);</li> <li>every <code>agent.id</code> is unique;</li> <li><code>training.step_cost &gt;= 0</code>;</li> <li><code>training.total_steps &gt; 0</code>;</li> <li><code>training.batch_size &gt; 0</code>.</li> </ul> <p>Invalid configs raise <code>pydantic.ValidationError</code>.</p>"},{"location":"contributing/","title":"Contributing","text":""},{"location":"contributing/#contributing-to-ddqn-router","title":"Contributing to ddqn-router","text":"<p>Thanks for your interest in improving <code>ddqn-router</code>! This guide walks through local setup, the PR workflow, and how releases are cut.</p>"},{"location":"contributing/#development-setup","title":"Development setup","text":"<pre><code>git clone https://github.com/kirmoz1997/ddqn-router\ncd ddqn-router\npython -m venv .venv &amp;&amp; source .venv/bin/activate\npip install -e \".[dev,serve]\"\npre-commit install\npytest\n</code></pre> <p>The <code>[dev]</code> extras pull in <code>pytest</code>, <code>pytest-cov</code>, <code>ruff</code>, <code>mypy</code>, <code>pre-commit</code>, <code>pandas</code>, and <code>matplotlib</code>. <code>[serve]</code> adds FastAPI + uvicorn.</p>"},{"location":"contributing/#branching-and-prs","title":"Branching and PRs","text":"<ul> <li>Branch from <code>main</code>. Name branches by purpose:   <code>feat/&lt;topic&gt;</code>, <code>fix/&lt;issue&gt;</code>, <code>docs/&lt;area&gt;</code>, <code>chore/&lt;cleanup&gt;</code>.</li> <li>Keep PRs focused and ideally under ~500 changed lines (excluding lock/test   fixture files).</li> <li>Use Conventional Commits:   <code>feat:</code>, <code>fix:</code>, <code>docs:</code>, <code>ci:</code>, <code>test:</code>, <code>build:</code>, <code>chore:</code>, <code>refactor:</code>.</li> <li>Before opening a PR, run <code>ruff check . &amp;&amp; ruff format --check . &amp;&amp; pytest -q   &amp;&amp; mypy ddqn_router/</code>.</li> </ul>"},{"location":"contributing/#running-checks-locally","title":"Running checks locally","text":"<pre><code>ruff check .\nruff format --check .\nmypy ddqn_router/\npytest -q\npytest --cov=ddqn_router --cov-report=term\n</code></pre> <p>You can reproduce CI locally with <code>act</code>:</p> <pre><code>act -j test\n</code></pre>"},{"location":"contributing/#code-style","title":"Code style","text":"<ul> <li>Target <code>py310+</code> syntax (the package still declares <code>requires-python &gt;= 3.9</code>,   but ruff/mypy are configured for <code>py310</code>).</li> <li>100-col line length, enforced by <code>ruff format</code>.</li> <li>Public API must be typed; CI runs <code>mypy --strict_optional</code>.</li> <li>No ad-hoc scripts \u2014 new functionality should land as CLI subcommands or   library APIs.</li> </ul>"},{"location":"contributing/#tests","title":"Tests","text":"<ul> <li>Tests live under <code>tests/</code> and mirror the package layout.</li> <li>Fixtures in <code>tests/conftest.py</code> include a tiny in-memory dataset and a   session-scoped <code>trained_artifacts</code> fixture (runs ~300 training steps once   per test session).</li> <li>Keep unit tests fast (&lt;1s each); use fixtures for shared state.</li> <li>Mark slow/integration tests with <code>@pytest.mark.slow</code> if added.</li> </ul>"},{"location":"contributing/#release-process","title":"Release process","text":"<p>Releases are automated by the <code>publish.yml</code> workflow:</p> <ol> <li>Update <code>CHANGELOG.md</code> \u2014 move <code>Unreleased</code> entries into a new versioned    section and set today's date.</li> <li>Bump <code>version</code> in <code>pyproject.toml</code> and <code>__version__</code> in    <code>ddqn_router/__init__.py</code>.</li> <li>Commit on <code>main</code>, tag, and push:    <pre><code>git commit -am \"chore: release v0.3.0\"\ngit tag v0.3.0\ngit push origin main --tags\n</code></pre></li> <li>The <code>Publish to PyPI</code> workflow runs on tag push and uploads to PyPI via    Trusted Publishing (OIDC).    Configure a Trusted Publisher for this repo on pypi.org once; no API token    needs to live in GitHub secrets.</li> <li>Draft a GitHub Release from the tag; the <code>Docker</code> workflow will then build    multi-arch images and push them to <code>ghcr.io/kirmoz1997/ddqn-router</code>.</li> </ol>"},{"location":"contributing/#first-time-pypi-setup-fallback","title":"First-time PyPI setup (fallback)","text":"<p>If Trusted Publishing isn't configured yet, add <code>PYPI_API_TOKEN</code> to repo secrets and uncomment the password line in <code>.github/workflows/publish.yml</code>.</p>"},{"location":"contributing/#reporting-issues","title":"Reporting issues","text":"<p>Use the GitHub issue templates under Issues \u2192 New. For security issues, see <code>SECURITY.md</code> \u2014 do not open a public issue.</p>"},{"location":"deployment/","title":"Deployment","text":"<p><code>ddqn-router serve</code> exposes a FastAPI app that loads a trained artifacts directory and answers routing requests. Inference is CPU-bound and sub-millisecond per query.</p>"},{"location":"deployment/#docker","title":"Docker","text":"<p>The published image is <code>ghcr.io/kirmoz1997/ddqn-router:&lt;version&gt;</code> (also <code>:latest</code>). Multi-arch (<code>linux/amd64</code>, <code>linux/arm64</code>).</p> <pre><code>docker run --rm -p 8000:8000 \\\n  -v \"$(pwd)/artifacts:/artifacts:ro\" \\\n  ghcr.io/kirmoz1997/ddqn-router:latest\n</code></pre> <pre><code>curl -s -X POST http://localhost:8000/route \\\n  -H 'content-type: application/json' \\\n  -d '{\"query\":\"my invoice was charged twice\"}' | jq\n</code></pre>"},{"location":"deployment/#docker-compose","title":"docker compose","text":"<pre><code>services:\n  router:\n    image: ghcr.io/kirmoz1997/ddqn-router:latest\n    ports: [\"8000:8000\"]\n    volumes:\n      - ./artifacts:/artifacts:ro\n</code></pre> <p><code>docker compose up -d</code> and you're serving.</p>"},{"location":"deployment/#bare-metal-fastapi","title":"Bare-metal FastAPI","text":"<pre><code>pip install \"ddqn-router[serve]\"\nddqn-router serve --artifacts ./artifacts --host 0.0.0.0 --port 8000\n</code></pre> <p>Behind a systemd unit, a Procfile, or your process manager of choice.</p>"},{"location":"deployment/#production-checklist","title":"Production checklist","text":"<ul> <li>TLS termination + authentication at the reverse proxy. The bundled   FastAPI app has no auth, no rate limiting, no metrics by design. Put   nginx/Caddy/Traefik/Cloudflare in front and enforce auth there.</li> <li>Restrict artifacts to read-only. The container volume mount already   uses <code>:ro</code>; do the same on bare metal.</li> <li>Resource limits. Inference is CPU-bound and fast; 1 CPU per ~1000   req/s is a reasonable starting point. Memory footprint is ~100\u2013300 MB.</li> <li>Warmup. The first request JIT-compiles torch ops. Fire a throwaway   request on boot (healthcheck <code>/health</code> does not trigger the model).</li> <li>Versioning. Pin the image to an explicit <code>v0.x.y</code> tag in production \u2014   don't use <code>:latest</code>.</li> <li>Observability. Log at the reverse-proxy layer. If you need custom   metrics, wrap the FastAPI app yourself (the app is returned by   <code>ddqn_router.serve.app.create_app</code>).</li> <li>Graceful rollouts. The artifacts directory is mounted read-only; to   roll out a new model, build a new image or swap the volume and restart.</li> </ul>"},{"location":"examples/","title":"Examples","text":""},{"location":"examples/#examples_1","title":"Examples","text":"<p>Reproducible end-to-end demos of <code>ddqn-router</code>.</p> Example Agents Time to run (CPU) Description <code>01_customer_support/</code> 5 ~5 min Customer-support triage across Billing, Technical, Account, Shipping, General. <code>02_it_helpdesk/</code> 5 ~5 min IT tier-1 helpdesk triage across Hardware, Network, Access, Software, Security. <code>colab_quickstart.ipynb</code> 3 ~5 min End-to-end notebook \u2014 works on a free Colab CPU with no API key required. <code>example_config.yaml</code> \u2014 \u2014 Annotated config reference. <code>raw_texts.txt</code> \u2014 \u2014 Legacy sample queries (kept for the Quickstart in the README). <p>Each subdirectory has its own <code>README.md</code> with step-by-step commands. All examples ship a small generator script instead of a large pre-labeled <code>queries.jsonl</code> to keep the repo lean \u2014 run <code>python make_dataset.py</code> inside the directory first.</p>"},{"location":"python-api/","title":"Python API","text":""},{"location":"python-api/#ddqnrouter","title":"<code>DDQNRouter</code>","text":"<pre><code>from ddqn_router import DDQNRouter, RouteResult, StepTrace\n\nrouter = DDQNRouter.load(\"./artifacts/\")\n</code></pre>"},{"location":"python-api/#ddqnrouterloadartifacts_path-ddqnrouter","title":"<code>DDQNRouter.load(artifacts_path) -&gt; DDQNRouter</code>","text":"<p>Loads a trained router. <code>artifacts_path</code> must contain <code>model.pt</code>, <code>encoder.joblib</code>, and <code>config_used.json</code>. Raises <code>RouterNotTrainedError</code> if any file is missing.</p>"},{"location":"python-api/#routerroutequery-str-routeresult","title":"<code>router.route(query: str) -&gt; RouteResult</code>","text":"<p>Runs a greedy routing episode; returns a <code>RouteResult</code>.</p>"},{"location":"python-api/#routerroute_batchqueries-liststr-listrouteresult","title":"<code>router.route_batch(queries: list[str]) -&gt; list[RouteResult]</code>","text":"<p>Sugar for <code>[router.route(q) for q in queries]</code>. Same semantics.</p>"},{"location":"python-api/#routerroute_verbosequery-str-routeresult","title":"<code>router.route_verbose(query: str) -&gt; RouteResult</code>","text":"<p>Same as <code>route</code>, but also populates <code>RouteResult.steps_trace</code> with a list of <code>StepTrace</code> entries \u2014 one per routing step.</p>"},{"location":"python-api/#routerexplainquery-str-none","title":"<code>router.explain(query: str) -&gt; None</code>","text":"<p>Pretty-prints a step-by-step Q-value table. Output format is stable across versions.</p>"},{"location":"python-api/#routeragents-listdict","title":"<code>router.agents -&gt; list[dict]</code>","text":"<p>Configured agents: <code>[{\"id\": ..., \"name\": ..., \"description\": ...}, \u2026]</code>.</p>"},{"location":"python-api/#routeresult","title":"<code>RouteResult</code>","text":"<pre><code>@dataclass\nclass RouteResult:\n    agents: list[int]           # selected agent IDs, in selection order\n    agent_names: list[str]      # parallel list of agent names\n    confidence: float           # scalar in [0, 1] (see below)\n    steps: int                  # number of routing steps taken\n    steps_trace: list[StepTrace] | None = None  # populated by route_verbose\n</code></pre>"},{"location":"python-api/#steptrace","title":"<code>StepTrace</code>","text":"<pre><code>@dataclass(frozen=True)\nclass StepTrace:\n    step_index: int                   # 0-based index of this step\n    q_values: dict[int, float]        # Q-value per valid (non-masked) action\n    selected_action: int              # chosen action id (num_agents == STOP)\n    stop_selected: bool               # True if the STOP action was taken\n    masked_agents: list[int]          # agent IDs masked out at this step\n</code></pre>"},{"location":"python-api/#how-confidence-is-computed","title":"How <code>confidence</code> is computed","text":"<p><code>confidence</code> is a scalar in <code>[0, 1]</code> derived from the last routing step's Q-value distribution:</p> <pre><code># ddqn_router/inference/router.py (line 253)\nconfidence = clip(\n    (max_q - mean_q) / (max_q - min_q + 1e-8),\n    0.0, 1.0,\n)\n</code></pre> <p>where <code>max_q</code>, <code>min_q</code>, and <code>mean_q</code> are computed over the Q-values of the valid (non-masked) actions at the final step of the routing episode. Returns <code>0.0</code> if no steps were taken and <code>1.0</code> if fewer than two actions remain valid (i.e. nothing to be uncertain between).</p> <p>Interpretation:</p> <ul> <li>Close to 1.0 means the argmax Q-value stood out sharply from the rest   of the valid options at the stopping step \u2014 the policy was decisive.</li> <li>Close to 0.0 means the valid Q-values were clustered tightly around   the mean \u2014 several candidates were roughly equally good (or bad).</li> </ul> <p>This is not a probability and it is not calibrated. It reflects Q-value dispersion only; two models with identical accuracy may report different confidence scales because Q-values depend on the reward structure. If you need calibrated probabilities, post-hoc methods (Platt / isotonic) are out of scope for this library \u2014 see the \"Out of scope\" section in the productionization plan.</p>"},{"location":"python-api/#exceptions","title":"Exceptions","text":"<ul> <li><code>RouterNotTrainedError</code> \u2014 raised by <code>DDQNRouter.load()</code> when any artifact   is missing. The message includes actionable next steps.</li> </ul>"},{"location":"quickstart/","title":"Quickstart","text":"<p>Go from an empty folder to a running <code>/route</code> endpoint in ~10 minutes.</p>"},{"location":"quickstart/#1-install","title":"1. Install","text":"<pre><code>pip install \"ddqn-router[serve]\"\n</code></pre>"},{"location":"quickstart/#2-scaffold-a-project","title":"2. Scaffold a project","text":"<pre><code>ddqn-router init --path my-router\ncd my-router\n</code></pre> <p>This creates:</p> <pre><code>my-router/\n\u251c\u2500\u2500 config.yaml\n\u251c\u2500\u2500 data/queries.example.txt\n\u251c\u2500\u2500 .gitignore\n\u2514\u2500\u2500 README.md\n</code></pre>"},{"location":"quickstart/#3-label-your-data-or-skip-to-use-the-shipped-examples","title":"3. Label your data (or skip to use the shipped examples)","text":"<pre><code>export DDQN_ROUTER_API_KEY=sk-...\n\nddqn-router label \\\n  --config config.yaml \\\n  --input data/queries.example.txt \\\n  --output data/tasks.jsonl\n</code></pre> <p>The labeler calls any OpenAI-compatible endpoint (OpenAI, DeepSeek, Ollama, \u2026) and caches each result so re-runs are cheap. Fallbacks (<code>keyword</code>, <code>all-agents</code>, <code>skip</code>) handle malformed LLM responses.</p>"},{"location":"quickstart/#4-split-and-train","title":"4. Split and train","text":"<pre><code>ddqn-router dataset split --input data/tasks.jsonl\nddqn-router train --config config.yaml\n</code></pre> <p>Artifacts land in <code>./artifacts/</code>: <code>model.pt</code>, <code>encoder.joblib</code>, <code>config_used.json</code>, <code>metrics_test.json</code>, <code>training_log.jsonl</code>, and a rolling <code>checkpoint/</code> directory.</p>"},{"location":"quickstart/#5-use-the-router","title":"5. Use the router","text":"PythonCLI evaluationHTTP serving <pre><code>from ddqn_router import DDQNRouter\n\nrouter = DDQNRouter.load(\"./artifacts/\")\nresult = router.route(\"my invoice was charged twice\")\nprint(result.agents, result.agent_names, result.confidence)\n</code></pre> <pre><code>ddqn-router eval --artifacts ./artifacts/ --input data/test.jsonl\n</code></pre> <pre><code>ddqn-router serve --artifacts ./artifacts/ --port 8000\ncurl -s -X POST http://localhost:8000/route \\\n  -H 'content-type: application/json' \\\n  -d '{\"query\":\"my invoice was charged twice\"}' | jq\n</code></pre>"},{"location":"troubleshooting/","title":"Troubleshooting","text":""},{"location":"troubleshooting/#routernottrainederror-no-trained-ddqn-router-found-in","title":"<code>RouterNotTrainedError: No trained DDQN router found in \u2026</code>","text":"<p>The artifacts directory is missing <code>model.pt</code>, <code>encoder.joblib</code>, or <code>config_used.json</code>. Run <code>ddqn-router train --config config.yaml</code> and check that <code>output_dir</code> matches the path you're loading from.</p>"},{"location":"troubleshooting/#validationerror-datasettrain_ratio-val_ratio-test_ratio-must-sum-to-10","title":"<code>ValidationError: dataset.train_ratio + val_ratio + test_ratio must sum to 1.0</code>","text":"<p>Your config has ratios that don't add up. <code>0.7 / 0.15 / 0.15</code> is the default.</p>"},{"location":"troubleshooting/#valueerror-duplicate-agent-id-x","title":"<code>ValueError: Duplicate agent id: X</code>","text":"<p>Two agents share the same <code>id</code>. Ids must be unique and contiguous starting from 0.</p>"},{"location":"troubleshooting/#valueerror-agent-ids-must-be-contiguous-without-gaps","title":"<code>ValueError: Agent ids must be contiguous without gaps</code>","text":"<p>You skipped an id (e.g. <code>0, 2, 3</code>). Renumber so ids are <code>0..N-1</code>.</p>"},{"location":"troubleshooting/#training-is-slow-cpu-pegged","title":"Training is slow / CPU pegged","text":"<ul> <li>Lower <code>training.total_steps</code> and <code>tfidf_max_features</code> for quick iteration.</li> <li>Check <code>training.min_replay_size</code> \u2014 warm-up steps don't update weights.</li> <li>GPU is supported automatically (<code>torch.cuda.is_available()</code>), but TF-IDF   preprocessing is CPU-only and tends to dominate small runs.</li> </ul>"},{"location":"troubleshooting/#cuda-out-of-memory","title":"CUDA out of memory","text":"<p>Reduce <code>training.batch_size</code> or <code>hidden_layers</code>, or run on CPU:</p> <pre><code>CUDA_VISIBLE_DEVICES=\"\" ddqn-router train --config config.yaml\n</code></pre>"},{"location":"troubleshooting/#labeler-api-errors","title":"Labeler API errors","text":"<ul> <li>Check <code>DDQN_ROUTER_API_KEY</code> / <code>labeler.api_key</code>.</li> <li>The labeler has three fallback strategies: <code>skip</code>, <code>keyword</code>,   <code>all-agents</code>. Pick one that's appropriate when the LLM returns malformed   output.</li> <li>Responses and errors are not cached \u2014 re-runs will retry.</li> </ul>"},{"location":"troubleshooting/#encoding-mismatch-at-inference","title":"Encoding mismatch at inference","text":"<p>The encoder must match the model. If you mix <code>encoder.joblib</code> from one run with <code>model.pt</code> from another, shape errors are raised. Always load the <code>artifacts/</code> directory produced by a single training run.</p>"},{"location":"troubleshooting/#route-returns-500","title":"<code>/route</code> returns 500","text":"<p>The server crashed during startup if artifacts are missing \u2014 check the uvicorn logs. If the server is up and a single request 500s, re-check the request schema: <code>{\"query\": \"&lt;string&gt;\"}</code>.</p>"},{"location":"troubleshooting/#resume-didnt-pick-up-where-i-left-off","title":"Resume didn't pick up where I left off","text":"<p><code>--resume</code> expects the checkpoint directory (<code>./artifacts/checkpoint/</code>), not the artifacts root. Confirm <code>step.json</code> exists inside the directory.</p>"}]}